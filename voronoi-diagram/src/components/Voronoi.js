import React from "react";
import { Box, Typography } from "@mui/material";

export default function Voronoi() {
  return (
    <Box>
      <Typography sx={{ color: "text.primary" }}>Voronoi</Typography>
    </Box>
  );
}
